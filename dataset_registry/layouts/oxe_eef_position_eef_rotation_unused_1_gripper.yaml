# Declared by the delivered dataset's own meta/modality.json. `unused_*`
# blocks are slots the 8-wide OXE state vector leaves empty; the original
# conversion pads to a common width.

order:
  - end_effector_position
  - end_effector_rotation
  - unused_1
  - gripper_position
