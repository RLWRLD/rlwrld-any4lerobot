"""Version conversion scripts.

A package only so that the test directories beneath it get import names of their
own. Without it every ``tests/`` in the repo is called ``tests``, and pytest
cannot have two.
"""
